# Domain News Layer

Папка `src/domain/news/` отвечает за логику и типы новостного раздела:
- Определение специфичных для новостей типов (`news.types.ts`).
- Функции загрузки новостей (`news.loader.ts`).
- React-хуки для работы с новостями в компонентах (`useNews.ts`).
